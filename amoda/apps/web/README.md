# AMODA Web

Next.js 15 frontend for the AMODA real estate platform. See the [root README](../../README.md) for setup,
environment variables, and architecture notes.

Quick start:

```bash
npm run dev:web            # from the amoda/ root
```

Runs at `http://localhost:3000`.
