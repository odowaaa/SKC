# AMODA API

NestJS backend for the AMODA real estate platform. See the [root README](../../README.md) for setup,
environment variables, and architecture notes.

Quick start:

```bash
npm run dev:api            # from the amoda/ root
```

- Swagger docs: `http://localhost:4000/api/docs`
- Health check: `GET /api/v1/health`
